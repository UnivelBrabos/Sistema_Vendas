<Window xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        x:Name="grdPrincipal" x:Class="Sistema_Vendas.View.MenuPrincipal"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:Sistema_Vendas.View"
        WindowStartupLocation="CenterScreen"
        HorizontalAlignment="Left"
        VerticalAlignment="Top"
        Title="Menu Principal"
        mc:Ignorable="d"
        MinHeight="720"
        MinWidth="1240" HorizontalContentAlignment="Center" Loaded="grdPrincipal_Loaded"
        >

    <Grid Margin="0,0,0,0">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="719*"/>
            <ColumnDefinition Width="521*"/>
        </Grid.ColumnDefinitions>

        <Grid Name="grdMenuLateral" Background="#FF73B8DA" HorizontalAlignment="Left" Width="194">
            <Image Source="/Imagens/Vendas_Icon.png" Stretch="Fill" HorizontalAlignment="Center" VerticalAlignment="Top" Height="69" Width="122"/>

            <Grid Name="grdDashboard">
                <Image x:Name="imgDashboard" Margin="16,143,0,0" Source="/Imagens/Dashboard_Icon.png" Stretch="Fill" HorizontalAlignment="Left" VerticalAlignment="Top" Height="35" Width="35"/>
                <Label x:Name="lblDashboard" Content="Dashboard" HorizontalAlignment="Left" Margin="59,148,0,0" VerticalAlignment="Top" Width="119" FontFamily="Rockwell" FontSize="14"/>

                <Grid Name="grdVisibilityDashboard" Visibility="Visible">
                    <Rectangle x:Name="retDashboard" Visibility="Hidden" HorizontalAlignment="Left" Height="36" Margin="0,143,0,0" Stroke="Black" VerticalAlignment="Top" Width="3" Fill="Black"/>
                    <Button x:Name="btnDashboard" HorizontalAlignment="Left" Margin="0,143,0,0" VerticalAlignment="Top" Width="191" Height="36" Background="Black" BorderBrush="{x:Null}" Cursor="Hand" Opacity="0">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Background" Value="Transparent"/>
                                <Setter Property="BorderThickness" Value="0"/>
                                <Setter Property="RenderTransformOrigin" Value="0.5,0.5"/>
                                <Setter Property="Cursor" Value="Hand"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Border Background="{TemplateBinding Background}" CornerRadius="2" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}">
                                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                                <Style.Triggers>
                                    <Trigger Property="IsPressed" Value="True">
                                        <Setter Property="RenderTransform">
                                            <Setter.Value>
                                                <ScaleTransform ScaleX="0.95" ScaleY="0.95"/>
                                            </Setter.Value>
                                        </Setter>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>
                </Grid>
            </Grid>

            <Grid Name="grdAuditoria">
                <Image Margin="16,233,0,0" Source="/Imagens/Auditoria_Icon.png" Stretch="Fill" HorizontalAlignment="Left" VerticalAlignment="Top" Height="35" Width="35"/>
                <Label x:Name="lblAudiotoria" Content="Auditoria" HorizontalAlignment="Left" Margin="59,238,0,0" VerticalAlignment="Top" Width="119" FontFamily="Rockwell" FontSize="14"/>

                <Grid Name="grdVisibilityAuditoria" Visibility="Visible">
                    <Rectangle x:Name="retAuditoria" HorizontalAlignment="Left" Height="36" Margin="0,233,0,0" Stroke="Black" VerticalAlignment="Top" Width="3" Fill="Black" Visibility="Hidden"/>
                    <Button x:Name="btnAuditoria" HorizontalAlignment="Left" Margin="3,233,0,0" VerticalAlignment="Top" Width="191" Height="36" Background="Black" BorderBrush="Black"  Opacity="0.0" Cursor="Hand">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Background" Value="Transparent"/>
                                <Setter Property="BorderThickness" Value="0"/>
                                <Setter Property="RenderTransformOrigin" Value="0.5,0.5"/>
                                <Setter Property="Cursor" Value="Hand"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Border Background="{TemplateBinding Background}" CornerRadius="2" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}">
                                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                                <Style.Triggers>
                                    <Trigger Property="IsPressed" Value="True">
                                        <Setter Property="RenderTransform">
                                            <Setter.Value>
                                                <ScaleTransform ScaleX="0.95" ScaleY="0.95"/>
                                            </Setter.Value>
                                        </Setter>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>
                </Grid>
            </Grid>

            <Grid Name="grdFuncionarios">
                <Image Margin="16,324,0,0" Source="/Imagens/Funcionarios_Icon.png" Stretch="Fill" HorizontalAlignment="Left" VerticalAlignment="Top" Height="35" Width="35"/>
                <Label x:Name="lblFuncionarios" Content="Funcionários" HorizontalAlignment="Left" Margin="60,327.3,0,0" VerticalAlignment="Top" Width="118" FontFamily="Rockwell" FontSize="14"/>

                <Grid Name="grdVisibilityFuncionarios" Visibility="Visible">
                    <Rectangle x:Name="retFuncionarios" Visibility="Hidden" HorizontalAlignment="Left" Height="36" Margin="0,324,0,0" Stroke="Black" VerticalAlignment="Top" Width="3" Fill="Black"/>
                    <Button x:Name="btnFuncionarios" Background="Black" BorderBrush="Black" Opacity="0.0" HorizontalAlignment="Left" Margin="3,324,0,0" VerticalAlignment="Top" Width="191" Height="36" Cursor="Hand">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Background" Value="Transparent"/>
                                <Setter Property="BorderThickness" Value="0"/>
                                <Setter Property="RenderTransformOrigin" Value="0.5,0.5"/>
                                <Setter Property="Cursor" Value="Hand"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Border Background="{TemplateBinding Background}" CornerRadius="2" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}">
                                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                                <Style.Triggers>
                                    <Trigger Property="IsPressed" Value="True">
                                        <Setter Property="RenderTransform">
                                            <Setter.Value>
                                                <ScaleTransform ScaleX="0.95" ScaleY="0.95"/>
                                            </Setter.Value>
                                        </Setter>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>
                </Grid>
            </Grid>

            <Grid Name="grdEstoque">
                <Image Margin="16,415,0,0" Source="/Imagens/Estoque_Icon.png" Stretch="Fill" HorizontalAlignment="Left" VerticalAlignment="Top" Height="35" Width="35"/>
                <Label x:Name="lblEstoque" Content="Estoque" HorizontalAlignment="Left" Margin="56,419,0,0" VerticalAlignment="Top" Width="118" FontFamily="Rockwell" FontSize="14"/>

                <Grid Name="grdVisibilityEstoque" Visibility="Visible">
                    <Rectangle x:Name="retEstoque" Visibility="Hidden" HorizontalAlignment="Left" Height="36" Margin="0,415,0,0" Stroke="Black" VerticalAlignment="Top" Width="3" Fill="Black"/>
                    <Button x:Name="btnEstoque" HorizontalAlignment="Left" Margin="3,415,0,0" VerticalAlignment="Top" Width="191" Height="36" Background="Black" BorderBrush="Black" Opacity="0.0" Cursor="Hand">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Background" Value="Transparent"/>
                                <Setter Property="BorderThickness" Value="0"/>
                                <Setter Property="RenderTransformOrigin" Value="0.5,0.5"/>
                                <Setter Property="Cursor" Value="Hand"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Border Background="{TemplateBinding Background}" CornerRadius="2" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}">
                                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                                <Style.Triggers>
                                    <Trigger Property="IsPressed" Value="True">
                                        <Setter Property="RenderTransform">
                                            <Setter.Value>
                                                <ScaleTransform ScaleX="0.95" ScaleY="0.95"/>
                                            </Setter.Value>
                                        </Setter>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>
                </Grid>
            </Grid>

            <Grid>
                <Image Margin="16,0,0,10" Source="/Imagens/Settings_Icon.png" Stretch="Fill" HorizontalAlignment="Left" Width="35" Height="35" VerticalAlignment="Bottom"/>
                <Grid Name="grdVisibilityConfiguracoes">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="682*"/>
                        <RowDefinition Height="23*"/>
                    </Grid.RowDefinitions>
                    <Button     x:Name="btnConfiguracoes"
                                HorizontalAlignment="Right"
                                Margin="0,0,142,10"
                                Width="35"
                                Background="Black"
                                BorderBrush="Black"
                                Opacity="0.0" Grid.RowSpan="2" Height="35" VerticalAlignment="Bottom" Cursor="Hand">

                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border Background="{TemplateBinding Background}"
                                        BorderBrush="{TemplateBinding BorderBrush}"
                                        BorderThickness="1"
                                        CornerRadius="4">

                                    <ContentPresenter   HorizontalAlignment="Center"
                                                    VerticalAlignment="Center" />
                                </Border>
                            </ControlTemplate>
                        </Button.Template>
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Background" Value="Transparent"/>
                                <Setter Property="BorderThickness" Value="0"/>
                                <Setter Property="RenderTransformOrigin" Value="0.5,0.5"/>
                                <Setter Property="Cursor" Value="Hand"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Border Background="{TemplateBinding Background}" CornerRadius="2" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}">
                                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                                <Style.Triggers>
                                    <Trigger Property="IsPressed" Value="True">
                                        <Setter Property="RenderTransform">
                                            <Setter.Value>
                                                <ScaleTransform ScaleX="0.95" ScaleY="0.95"/>
                                            </Setter.Value>
                                        </Setter>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>
                </Grid>
            </Grid>
        </Grid>

        <Grid Name="grdConteudo" Margin="194,68,0,0" Grid.ColumnSpan="2">
            <Label x:Name="lblFiltros" Content="Filtros&#xD;&#xA;" HorizontalAlignment="Center" VerticalAlignment="Top" RenderTransformOrigin="0.566,0.499" Width="104" HorizontalContentAlignment="Center" FontSize="22" FontFamily="Rockwell" Height="35"/>
            <DatePicker x:Name="dtpInicial" HorizontalAlignment="Left" Margin="243,63,0,0" VerticalAlignment="Top" Background="{x:Null}" Foreground="#FF333333" BorderBrush="{x:Null}" FontFamily="Rockwell" Width="228" FontSize="20" Height="33"/>
            <DatePicker x:Name="dtpFinal" HorizontalAlignment="Right" Margin="0,63,242,0" VerticalAlignment="Top" Background="{x:Null}" Foreground="#FF333333" BorderBrush="{x:Null}" FontFamily="Rockwell" Width="229" Height="33" FontSize="20"/>
            <Label x:Name="lblAte" Content="Até" HorizontalAlignment="Center" Margin="0,63,0,0" VerticalAlignment="Top" HorizontalContentAlignment="Center" Width="104" FontSize="20" FontFamily="Rockwell"/>
            <Rectangle x:Name="retDiv1" Height="2" Margin="0,107,0,0" Stroke="Black" VerticalAlignment="Top" Width="797" RenderTransformOrigin="0.5,0.5">
                <Rectangle.RenderTransform>
                    <TransformGroup>
                        <ScaleTransform/>
                        <SkewTransform AngleX="-0.286"/>
                        <RotateTransform/>
                        <TranslateTransform X="-0.008"/>
                    </TransformGroup>
                </Rectangle.RenderTransform>
            </Rectangle>
            <ComboBox x:Name="cmbVendedores"
                HorizontalAlignment="Right"
                Margin="0,192,242,0"
                VerticalAlignment="Top"
                Width="229"
                ItemsSource="{Binding ItemscCheckBox}"
                FontFamily="Rockwell" Background="{x:Null}" BorderBrush="{x:Null}" FontSize="20" Height="29">
                <ComboBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal">
                            <CheckBox IsChecked="{Binding IsSelected}" Content="{Binding Name}" />
                        </StackPanel>
                    </DataTemplate>
                </ComboBox.ItemTemplate>
            </ComboBox>
            <TextBlock x:Name="txtVendedores" Margin="243,192,0,0" TextWrapping="Wrap" FontFamily="Rockwell" TextAlignment="Center" HorizontalAlignment="Left" Width="228" Height="29" VerticalAlignment="Top" FontSize="20"><Run Language="pt-br" Text="Vendedores"/></TextBlock>
            <Rectangle x:Name="retDiv2" Height="2" Margin="124,239,124,0" Stroke="Black" VerticalAlignment="Top" Width="797" RenderTransformOrigin="0.5,0.5">
                <Rectangle.RenderTransform>
                    <TransformGroup>
                        <ScaleTransform/>
                        <SkewTransform AngleX="-0.286"/>
                        <RotateTransform/>
                        <TranslateTransform X="-0.008"/>
                    </TransformGroup>
                </Rectangle.RenderTransform>
            </Rectangle>
            <TextBlock x:Name="txtClientes" Margin="243,335,0,0" TextWrapping="Wrap" FontFamily="Rockwell" TextAlignment="Center" HorizontalAlignment="Left" Width="228" Height="29" VerticalAlignment="Top" Text="Clientes" FontSize="20"/>
            <ComboBox x:Name="cmbClientes"
                HorizontalAlignment="Right"
                Margin="0,332,242,0"
                VerticalAlignment="Top"
                Width="229"
                ItemsSource="{Binding ItemsClientesCheckBox}"
                FontFamily="Rockwell" Background="{x:Null}" BorderBrush="{x:Null}" FontSize="20">
                <ComboBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal">
                            <CheckBox IsChecked="{Binding IsSelected}" Content="{Binding Name}" />
                        </StackPanel>
                    </DataTemplate>
                </ComboBox.ItemTemplate>
            </ComboBox>
            <Rectangle x:Name="retDiv2_Copiar" Height="2" Margin="125,380,124,0" Stroke="Black" VerticalAlignment="Top" Width="797" RenderTransformOrigin="0.5,0.5">
                <Rectangle.RenderTransform>
                    <TransformGroup>
                        <ScaleTransform/>
                        <SkewTransform AngleX="-0.286"/>
                        <RotateTransform/>
                        <TranslateTransform X="-0.008"/>
                    </TransformGroup>
                </Rectangle.RenderTransform>
            </Rectangle>
            <Button x:Name="btnFiltrar_Copiar" Content="Filtrar" HorizontalAlignment="Left" Margin="126,0,0,19" Width="101" Background="#FF8DED88" BorderBrush="{x:Null}" FontFamily="Rockwell" Click="btnFiltrar_Click" Height="34" VerticalAlignment="Bottom" FontSize="20">
                <Button.Style>
                    <Style TargetType="{x:Type Button}">
                        <Setter Property="Background" Value="Transparent"/>
                        <Setter Property="BorderThickness" Value="0"/>
                        <Setter Property="RenderTransformOrigin" Value="0.5,0.5"/>
                        <Setter Property="Cursor" Value="Hand"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="{x:Type Button}">
                                    <Border Background="{TemplateBinding Background}" CornerRadius="2" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}">
                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                        <Style.Triggers>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="RenderTransform">
                                    <Setter.Value>
                                        <ScaleTransform ScaleX="0.95" ScaleY="0.95"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>
            <Button x:Name="btnLimpar" Content="Limpar" HorizontalAlignment="Right" Margin="0,0,124,19" Width="101" Background="#FFED8888" BorderBrush="{x:Null}" FontFamily="Rockwell" Height="34" VerticalAlignment="Bottom" Click="btnLimpar_Click" FontSize="20">
                <Button.Style>
                    <Style TargetType="{x:Type Button}">
                        <Setter Property="Background" Value="Transparent"/>
                        <Setter Property="BorderThickness" Value="0"/>
                        <Setter Property="RenderTransformOrigin" Value="0.5,0.5"/>
                        <Setter Property="Cursor" Value="Hand"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="{x:Type Button}">
                                    <Border Background="{TemplateBinding Background}" CornerRadius="2" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}">
                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                        <Style.Triggers>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="RenderTransform">
                                    <Setter.Value>
                                        <ScaleTransform ScaleX="0.95" ScaleY="0.95"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>

        </Grid>

        <Grid Name="MenuHorizontal" VerticalAlignment="Top" Height="68" Margin="194,0,0,0" Background="#FFF0F0F0" Grid.ColumnSpan="2">
            <Image x:Name="imgUsuario" Margin="0,10,10,0" Source="/Imagens/User_Icon.png" Stretch="Fill" HorizontalAlignment="Right" VerticalAlignment="Top" Width="45" Height="45"/>
            <Image x:Name="imgFiltros" Margin="10,10,0,0" Source="/Imagens/Filtro_Icon.png" Stretch="Fill" Height="45" VerticalAlignment="Top" HorizontalAlignment="Left" Width="45"/>
            <Button x:Name="btnUsuario" Margin="0,10,10,0" Background="Black" BorderBrush="Black" Opacity="0.0" Cursor="Hand" Height="45" VerticalAlignment="Top" RenderTransformOrigin="0.5,0.5"  HorizontalAlignment="Right" Width="45">
                <Button.RenderTransform>
                    <TransformGroup>
                        <ScaleTransform ScaleX="-1"/>
                        <SkewTransform/>
                        <RotateTransform/>
                        <TranslateTransform/>
                    </TransformGroup>
                </Button.RenderTransform>
                <Button.Style>
                    <Style TargetType="{x:Type Button}">
                        <Setter Property="Background" Value="Transparent"/>
                        <Setter Property="BorderThickness" Value="0"/>
                        <Setter Property="RenderTransformOrigin" Value="0.5,0.5"/>
                        <Setter Property="Cursor" Value="Hand"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="{x:Type Button}">
                                    <Border Background="{TemplateBinding Background}" CornerRadius="2" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}">
                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                        <Style.Triggers>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="RenderTransform">
                                    <Setter.Value>
                                        <ScaleTransform ScaleX="0.95" ScaleY="0.95"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>
            <Label x:Name="lblNomeUsuario" Content="João da Silva" Margin="706,0,60,0" VerticalAlignment="Center" FontFamily="Rockwell" FontSize="14" HorizontalContentAlignment="Right"/>
            <Button x:Name="btnFiltrar" Margin="10,10,0,0" Background="Black" BorderBrush="Black" Opacity="0.0" Cursor="Hand" RenderTransformOrigin="0.5,0.5" Height="45" VerticalAlignment="Top" HorizontalAlignment="Left" Width="45">
                <Button.RenderTransform>
                    <TransformGroup>
                        <ScaleTransform ScaleX="1"/>
                        <SkewTransform/>
                        <RotateTransform/>
                        <TranslateTransform/>
                    </TransformGroup>
                </Button.RenderTransform>
                <Button.Style>
                    <Style TargetType="{x:Type Button}">
                        <Setter Property="Background" Value="Transparent"/>
                        <Setter Property="BorderThickness" Value="0"/>
                        <Setter Property="RenderTransformOrigin" Value="0.5,0.5"/>
                        <Setter Property="Cursor" Value="Hand"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="{x:Type Button}">
                                    <Border Background="{TemplateBinding Background}" CornerRadius="2" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}">
                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                        <Style.Triggers>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="RenderTransform">
                                    <Setter.Value>
                                        <ScaleTransform ScaleX="0.95" ScaleY="0.95"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>
        </Grid>
    </Grid>
</Window>